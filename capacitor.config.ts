import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.darthcompany.org',
  appName: 'polleria-YAMI-CHICKEN',
  webDir: 'dist'
};

export default config;
